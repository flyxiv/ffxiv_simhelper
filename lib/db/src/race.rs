use crate::stat::*;
use std::collections::HashMap;

pub type RaceId = usize;

/// Saves information needed about each race.
#[derive(PartialEq, Eq)]
pub struct Race {
    race_name: String,
    strength:
}

